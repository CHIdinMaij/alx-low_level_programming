#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int ch;

	for (ch = 0; ch	< 100; ch++)

		{

	putchar((d / 10));
	putchar((d % 10));
	if (d != 99)
	{
		putchar(',');
		putchar(' ');
		}

	}


	putchar('\n');

	return (0);

}
