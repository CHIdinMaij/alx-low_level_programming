#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char low = 'a'


	while (low <= 'z' )
	{
		if (low != 'e' && low != 'q')
                	putchar(low);
	}
	        printf("\n");
	
	return (0);

}
